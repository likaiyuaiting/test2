#include <stdio.h>

/*
 * j 下
 * k 上
 * h 左
 * l 右
 * $ 行尾
 * ^ 行首
 * w 下一个单词开始
 * b 上一个单词开始
 * H 当前屏幕的第一行
 * L 最下一行
 * M 当前屏幕的中间
 * :set nu 显示行号
 * :$当前文件的最后一行
 * :n 到第n行
 * { 上一个空行
 * } 下一个空行
 * 
 * nx 删除光标所在位置的n个字符
 * u 撤销
 * ndd 删除n行
 * p 粘帖
 * nyy 复制n行
 * nyw 复制n个单词
 * ndw 删除n个单词
 * r 替换一个字符
 * R 进入替换模式，替换完成后esc退出
 * D | d$ 删除从光标所在位置到当前行的结尾
 * d^ 删除光标所在位置到当前行的行首
 * dG 删除从光标所在行到文件结尾
 * d} 删除从光标所在位置到下一个空白行
 * d{ 删除从光标所在位置到上一个空白行
 *
 * v 进入可视化模式
 * 	y 拷贝
 * 	x 删除
 * 	shift < 向左缩进
 * 	shift > 向右推进
 *
 * :vsp my_math.h
 *     ctrl + ww 切换屏幕
 *
 *  / 查找
 *     n 查找下一个
 *     N 查找上一个
 *  :start,end s/old/new/g
 *  :%s/;/\r{\r}\r/
 */ 

int sub(int a, int b)
{
	for (int i=0; i<10; i++) 
	{
		for( j=0; j<10; j++)
		{
			if ( XX )
			{
			}
			else
			{
			}

		}
	}
	return a - b;
}

int main( void )
{
	printf("this is maomaochong\n");
}

