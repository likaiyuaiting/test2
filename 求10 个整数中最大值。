//求10 个整数中最大值。
#include<stdio.h>
#include<stdlib.h>

int main(void)
{
	int a[10] = {1,4,5,7,8,9,3,6,2,0};
	int max;
	for (int i = 0; i < 10;i++)
	if (i == 0)
		max = a[i];
	else if (a[i]>max)
		max = a[i];
	printf("max=%d\n",max);
	system("pause");
	return 0;
}
